const names = ['Ulan', 'Medet', 'Tima', 'Sam', 'Askar', 'Akin'];

console.log(names);

// make bad formatted to see prettier effect
function a(x, y) {
    return x + y;
}

// uncomment to see an effect
a(1, 2);
